# Shared contracts

Versioned schemas for Study, Series, AnatomySegment, PathologyFinding, Measurement, Uncertainty, LongitudinalDelta, ClinicalReport, and ModelProvenance belong here.

Keep model outputs deterministic and machine-readable. The report generator consumes structured outputs rather than raw pixels.
